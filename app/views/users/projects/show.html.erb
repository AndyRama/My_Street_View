<div class="background">
  <div class="box">
    <div class="image-title">
      <%= cl_image_tag @project.photo.key, height: 180, width: 286 %>
      <h1 class="title"> <%= @project.title %> </h1>
    </div>
  <!--   <div class="problematic">
      <h3 class="problematic-title">Problématique</h3>
      <p class="problematic-description"> <#%= @project.problematicbody %> </p>
    </div> -->
    <div class="description">
      <h5>Description</h5>
      <p class="descriptionbody"> <%= @project.description %> </p>
    </div>
    <div class="footer">
      <% @questions.each do |question| %>
        <%= question.title %>
        <%= link_to "Donnez votre avis", new_question_feedback_path(question), class: "btn btn-blue" %>
      <% end %>
      <div class="back">
        <%= link_to "Retour", projects_path %>
      </div>
    </div>
  </div>
</div>
