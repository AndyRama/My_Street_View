<div class="background">
  <div class="box shadow-lg p-3 mb-5 bg-white rounded">
    <div class="container">
        <h3><%= @question.title %></h3>

        <%= simple_form_for [:users, @question, @feedback] do |f| %>
         <%= f.error_notification %>
        <div class="form-inputs">
          <%= f.input :answer, label: 'Make a choice', as: :radio_buttons, collection: [
            @question.first_answer, @question.second_answer] %>
        </div>

        <div class="form-actions">
            <%= f.button :submit, "Submit", class: "btn btn-login m-3" %>
        </div>
          <%= link_to "Answer later", :back %>
      <% end %>
    </div>
  </div>
</div>
