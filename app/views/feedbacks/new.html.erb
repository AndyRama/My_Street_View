<div class="background">
  <div class="login-box shadow-lg p-3 mb-5 bg-white rounded">
    <div class="container">
        <h3><%= @question.title %></h3>

        <%= simple_form_for [@question, @feedback] do |f| %>
        <div class="form-inputs">
          <%= f.input :answer, label: 'Make a choice', as: :radio_buttons, collection: [
            @question.first_answer, @question.second_answer] %>
        </div>

        <div class="form-actions">
          <%= link_to project_path(@question.project) do %>
            <%= f.button :submit, "Submit", class: "btn btn-login m-3" %>
          <% end %>
        </div>
          <%= link_to link_to "Answer later", :back %>
      <% end %>
    </div>
  </div>
</div>
